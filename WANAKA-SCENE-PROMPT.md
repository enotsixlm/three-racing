# 场景复刻 Prompt：北京纸模卡丁车赛道（three-racing → Wanaka）

> 把下面这份规格作为 prompt 交给 Wanaka（scene copilot / agent），即可复刻 three-racing 的整张场景。
> 所有数据均从原作源码（`track.js` / `beijing.js` / `paper.js` / `car.js` / `main.js`）提取，单位为**米**，
> 颜色为原作十六进制。落地到 Wanaka 时按「引擎换算约定」一节转换。

---

## 0. 一句话总览

马里奥赛车画风的 **3D 卡丁车闭环赛道**，铺在一张**纸模手工风的北京城市微缩沙盘**上：
灰卡纸地面、剪纸树、纸板山圈围成立体书舞台，赛道穿过天安门、鸟巢、水立方、央视大楼、
中国尊、天坛、国家大剧院、白塔胡同等地标，沿途红灯笼路灯与金币。

**美术三关键词：纸模（papercraft）、两阶平涂（two-step toon）、剪纸白边（white cut-paper outline）。**

## 1. 引擎换算约定（Wanaka 落地时用）

- **比例**：1 米 = 100 引擎单位。本文所有长度×100、坐标×100 后使用。
- **坐标系**：Y-up；本文用俯视 (x, z) 平面坐标，y 为高度。原作与 Wanaka 的 x/z 轴一致。
- **朝向**：原作 heading 以 +Z 为 0、逆时针为正（`forward = (sin h, cos h)`）；
  Wanaka yaw=0 朝 **-Z**、`forward = (sin yaw, -cos yaw)` → **换算 `yaw = 180° − heading°`**。
- **原语映射**：方盒 → `obj.Cube3D`；路面 → `obj.Road3D`（闭环 waypoints）；
  圆柱/球/圆锥/圆环无内建原语 → 用 catalog 基础形状 GLB（如 ball/box）、Tripo 生成或方盒近似
  （纸模风格下八棱柱/方盒近似完全可接受）。
- **禁忌**：GLB 资产不得带 `TANGENT` / `KHR_texture_transform`（dev WebGPU 会崩 TSL composite）。

## 2. 天空 / 光照 / 雾

| 项 | 原作值 | Wanaka 建议 |
|---|---|---|
| 天空底色 | `#b5d9ec`（浅粉蓝） | `background: {r:181,g:217,b:236}` + `weather.skyTint:"#b5d9ec"` |
| 雾 | 线性雾 `#d3e7f0`，320m→1400m | `fx.fog` 或远景靠山圈遮挡即可 |
| 半球光 | 天 `#d6ecff` / 地 `#6fc255`，强度 1.1 | `weather:{type:'clear', timeOfDay:12, brightness≈1.1}` |
| 太阳直射光 | `#fff4da`，强度 1.7，来自玩家东偏北上方（+60,+95,+35 偏移），带阴影 | `shadowQuality:'medium'`；注意压曝光防 washout |
| 色调映射 | ACES，曝光 1.2 | 引擎默认过曝时把 brightness 往下调 |

## 3. 赛道（核心）

**闭环 CatmullRom 曲线**（tension 0.5，居中采样 420 点），14 个控制点（米，俯视 x,z）：

```
(0,-80) (60,-70) (100,-30) (90,20) (120,60) (90,100) (30,90)
(0,130) (-50,120) (-90,70) (-70,20) (-110,-20) (-80,-70) (-30,-90)
```

- **路面**：半宽 7m（全宽 14m），沥青灰 `#6e7480`，贴地 y≈0.01。
  Wanaka 首选：`obj.Road3D` + `waypoints`（上表×100）+ `closed:true` + `width:1400`，profile `track`。
- **中央黄虚线**：`#ffe066`，段长 2m × 宽 0.28m，约每 2m 一段（420 采样每 4 点放 1 段）。
- **两侧白实线**：路缘内 0.25m 处（±6.75m）。（Road3D 的 track 纹理自带标线时可省略）
- **红白路肩**：只出现在弯道（前后 ±4 采样内曲率 > 0.1 rad 处），
  0.9×0.1×4m 的盒子贴路缘外 0.45m（±7.45m），红 `#ff5d5d` / 白交替。
- **起跑线**：采样点 0 处（约 (0,-80)，切向朝 +x 偏北），12 列 × 2 行黑白格，格宽 14/12 m。
- **起点门**：路两侧 ±8.2m 立白色圆柱门柱（高 7m，半径 0.3），柱顶各飘 3 只气球
  （红 `#ff5d5d` 黄 `#ffd43b` 蓝 `#4dabf7`）；6.2m 高处横挂红底横幅 `#e63946`，
  白字 "S T A R T !"，上下缘黑白棋盘边。
- **隐形护栏**：路缘外 9m（中心线 ±16m）为硬边界——超出即贴墙滑动（只消除向外速度分量，不吸附）。

## 4. 地面

2000×2000m 灰卡纸：底色 `#e0ddd4`，散布深色纸纤维小斑点（`rgba(90,78,62,0.03~0.08)`），
以 **76.9m 见方**为一格描白色纸边（模拟一张张卡纸拼起来）。
Wanaka：大 Cube3D 顶面贴 256px 平铺贴图（`topFaceResourceRepeat:true`），或 GLB 平面烘 UV 重复。

## 5. 地标（位置 = 组原点，米）

> 通用工艺：全部由原语拼装、两阶 toon 平涂；金顶统一 `#d4ac0d`，宫墙红 `#b03a2e`，汉白玉 `#f5f1e8`。
> "四坡屋顶" = 4 段圆柱近似的方锥台（底边×1.42、顶边×topScale、旋转 45° 对齐、按进深压扁）。

| 地标 | 位置 (x,z) | 朝向 | 拼装配方（自下而上，局部坐标） |
|---|---|---|---|
| **天安门** | (0,-20) | 绕 Y 转 180°（面向南边起点直道） | 红城台 44×10×12 @y5，南面 5 个暗红门洞（中间 3.4×4.6 最大）；白玉栏杆层 45×1×13 @y10.5；红城楼 30×5.5×8 @y13.75；金色重檐：四坡顶 34×11×2.4(top0.72) @y17.7 + 红楼层 26×2.4×6.5 @y20.1 + 四坡顶 29×9.5×2.8(top0.28) @y22.7 |
| **旗杆+红旗** | (0,-52) | 旗面朝 +x | 银灰圆柱杆 r0.12/0.15 高 14；红旗 `#e03131` 3.4×2.2 平面 @(1.8, 12.8) |
| **鸟巢** | (40,185) | 整体 y 压扁 0.8 | 钢灰 `#aab4bd` 双环：躺平圆环 R24/管 5.5（y 向拉 1.25）@y7 + R19/管 3 @y11.5；红碗 `#c0392b` 圆台 r15→17 高 7 @y4 |
| **水立方** | (98,180) | — | 半透明冰蓝盒 30×11×30 `#9bd4ff` opacity 0.92，表面白描边气泡贴图（128px，26 个随机圆圈） |
| **央视大裤衩** | (175,15) | 绕 Y 转 -0.5rad | 深灰基座 42×6×18；两根 11×52×13 塔互相内倾 ±0.12rad @x±14；顶部横梁 32×10×13 @y58；悬挑块 12×16×13 @(6,45)；主体 `#7f8fa6` |
| **中国尊** | (155,-65) | — | 6 段 13m 高方盒叠塔，宽 17→14.5→12.5→11.5→12→13.5（中间收腰），`#9fb3c8`；金冠 10×3×10 |
| **CBD 楼群** | 绕 (160,-35) 半径 22~52 | 随机 | 9 栋 `#b9c7d6` 方楼，宽 8~16、高 22~56 |
| **天坛** | (-75,-165) | 整体 ×1.15 | 三层汉白玉圆台 r17/14/11 各高 1.5；朱红殿身圆柱 r6.5 高 4 @y6.5；三重琉璃蓝 `#274b8f` 圆锥檐 r9.5/7.5/5.5 @y9.2/11.6/14.6（下两层间夹朱红束腰）；金球顶 @y17 |
| **国家大剧院** | (-175,30) | — | 银灰 `#aeb9c2` 半椭球壳 20×11×15；正面嵌一条竖向玻璃带 `#5c7cfa`；周围 r27 浅蓝水池 `#74c0fc` |
| **胡同四合院** | (-42..-15, 18..27) 4+4+2 排布 | 每栋随机 ±0.15rad | 10 栋：灰墙 `#9aa0a6` 5~8×3×4~6 + 深瓦四坡顶 `#5d6d7e` + 朱红门板 1.2×2 |
| **北海白塔** | (-18,42) | — | 白 `#fafafa`：圆台基座 r4→5 高 2.5；覆钵球 r3.6（y×1.15）@y6；相轮颈锥 r0.9→1.6 高 3.5 @y10.5；金锥刹 r1.4 高 4 @y14 |

## 6. 环境装饰

- **环城楼群**：70 栋方楼撒在 640×640m 内（种子 424242 的 LCG 随机），
  距赛道 ≥ 21m、避开上表地标半径圈；宽/深 9~19m、高 10~34m；
  外墙贴 64×128px 窗格贴图（白底、深蓝窗 `#3d4a5c`、30% 暖黄亮窗 `#ffe8a3`、白色纸边描框），
  楼体从 5 色纸卡调色板轮换染色：`#f2e9dc #dde3ea #e8d3c3 #cfe0d8 #f5f5f5`，屋顶灰 `#8d99a6`。
- **红灯笼路灯**：沿赛道每 12 个采样点（≈17m）1 盏，左右交替，距路中心 9.6m：
  深灰杆高 5.2m + 红灯笼球 `#e03131`（r0.55，y×1.25，带暗红自发光）@y5.4 + 金穗 @y4.5。
- **剪纸树**：竖立的双面剪纸片（7.3×10m×缩放 0.8~1.6），三种绿色变体
  （树冠 `#4cae54/#63bd57/#3f9e5e` + 深色斑点 + 棕干 `#8a5a33`，图案外圈 6px 剪刀白边）；
  5 块绿卡纸圆草坪 `#7ecf63`（r16~26m）每块栽 3~5 棵 + 路边散栽 18 棵；微风下整树 ±3° 摇摆。
- **纸板山圈**（立体书舞台边界）：两圈竖立剪纸山片围场：
  远圈 8 片 640×280m @半径 860m（淡蓝灰 `#c7d8e2` + 白雪尖），
  近圈 8 片 500×220m @半径 680m（灰绿 `#9cb8a6` + 雪尖），全部朝向场地中心。
- **剪纸太阳**：12 芒剪纸日 `#ffd166`，95m 见方，挂在 (280,190,-340)。
- **剪纸云**：10 朵白色剪纸云（42~88m 宽，y 85~155），沿 +x 以 1.5~4 m/s 漂移，±520m 循环回绕。
- **金币**：36 枚金色 `#ffc933` 立式圆片（r0.55×厚 0.09，带暗金自发光），
  沿赛道每 11 个采样点 1 枚、在 −3.2/0/+3.2m 三车道轮换，悬浮 y=1.0，自转 + ±0.15m 上下浮动。

## 7. 视觉风格规则（贯穿一切）

1. **两阶平涂**：所有实体色只有亮/暗两档（toon gradient 150/255），无平滑过渡 → 纸片平涂感。
   Wanaka 可用 `materialEffect:"Toon"` 或直接无金属 Standard + 压低粗糙度变化近似。
2. **白色折痕棱线**：所有拼装几何体的硬棱（二面角 >26°）描 `#fffdf5`、85% 不透明的细白线 → 折纸折痕。
   （GLB 路线 = EdgesGeometry 烘进模型；纯原语路线可省略，靠白边贴图撑风格。）
3. **剪纸白边**：所有贴图图案（树/云/山/太阳）外圈 6~8px 白色描边，像剪刀剪出来的。
4. **禁高反射**：全场景零金属度、无环境反射，材质像卡纸。

## 8. 相机（追尾）

- 默认追尾位：车后 9.5m、高 4.2m，看向车前 4m、高 1.2m 处；位置以 ~6/s、注视点以 ~10/s 指数平滑。
- FOV 62°，随速度加宽至最多 +16°（速度感）。
- Wanaka：`mode:'follow', preset:'thirdPerson', follow:{distance≈1040, pitch≈17}, followRotation:'yaw-only'`，
  加速时可 `adjustFollowConfig` 拉远。

## 9. 卡丁车（可选：让场景可开）

**外观配方**（色 = 车队色，米）：底盘 1.6×0.42×2.9 @y0.5 + 前杠 + 椭球鼻锥 + 尾翼块；
深灰座椅；白衣车手（方躯干 + 肤色圆头 + 车队色半球帽 + 帽檐）；方向盘圆环；
4 只大轮：黑胎圆柱 r0.42×宽 0.42 + 灰毂（前轮随转向 ±0.5rad 摆动，全轮随速度自转）。
**车队**：玩家蓝 `#2e7dff`、AI 红 `#e53935`（技能 0.97）、黄 `#ffb300`（0.92）、紫 `#8e24aa`（0.87）。
**发车格**：起跑线后方（采样 N−8 起，每排后退 7 采样≈10m），左右交替偏 ±2.8m，玩家最后一排。

**街机手感真值**（米/秒制，Wanaka 落地×100 变 px/s）：
极速 47（草地 17）、加速 21、刹车 34、倒车极速 11、空气阻力系数 0.45（草地×2.2）、
轴距 2.7、最大前轮转角 0.55rad（高速衰减 `steer/(1+|v|·0.05)`）、
漂移（按住空格且 |v|>8）横向抓地从 9 降到 2.0 且偏航率 ×1.2、草地抓地 3.5；
离路面（|横向偏移|>7m）判定为草地。护栏规则见 §3 末行。

## 10. 验收清单（复刻完成的判定）

- [ ] 俯视小地图轮廓与 §3 的 14 控制点闭环一致，起跑线在南侧直道
- [ ] 站在起跑线：正前方（北）是天安门背面，起点门横幅 "S T A R T !" 在头顶
- [ ] 沿途能依次认出：鸟巢+水立方（北）、大裤衩+中国尊+CBD（东）、天坛（南）、大剧院蛋壳（西）、白塔+胡同（内圈）
- [ ] 红白路肩只在弯道出现；红灯笼灯柱左右交替
- [ ] 远景四周被两圈雪山纸板围住，天上有剪纸太阳和漂移的剪纸云
- [ ] 整体观感：卡纸拼贴 + 平涂 + 白边，无真实感 PBR 反射

---

*来源：`~/Projects/three-racing`（vite+three）。高保真资产路线（原几何直接导出的 23 个 GLB + 布局 manifest）已备份在 `export_out/`，与本 prompt 二选一或组合使用。*
